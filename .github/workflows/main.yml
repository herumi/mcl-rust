name: test
on: [push]
permissions:
  contents: read
jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: sudo apt update
    - run: git submodule update --init --recursive
    - run: cargo build
    - run: cargo test
